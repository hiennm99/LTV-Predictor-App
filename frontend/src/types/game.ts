export type Game = {
    id: number; // SERIAL -> number
    package_name: string; // date -> string (ISO date format)
    name: string; // date -> string (ISO date format)
    os: string; // timestamp -> string (ISO datetime format)
}